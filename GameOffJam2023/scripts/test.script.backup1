
display_message("Hello, world!");

for $number in range (1, 25) do
		$IDnumber = make4LenNum($number);
		if widget["Collected Fish"].element[str($IDnumber)].item == null then
			widget["Collected Fish"].element[str($IDnumber)].item = "ITEM_0001";
			break;
		end;
	end;
	//widget["widget_id"].element["make4LenNum($number)"]
	//make4LenNum($number);
 //if random(1, 20) > 15 & $caught == "true"  then

//elseif
//end;
	
